{
  "name": "napam",
  "version": "1.0.0",
  "description": "",
  "bin": "./bin.mjs",
  "type": "module",
  "scripts": {
    "build": "esbuild ./index.mts --bundle --platform=node --packages=external --outfile=bin.mjs --format=esm "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.18.10",
    "typescript": "^5.1.6",
    "zx": "^7.2.2"
  }
}
